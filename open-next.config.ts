// open-next.config.ts
// Configuration for @opennextjs/cloudflare - the actively maintained adapter
// that replaces the deprecated @cloudflare/next-on-pages.
import { defineCloudflareConfig } from '@opennextjs/cloudflare';

export default defineCloudflareConfig();
